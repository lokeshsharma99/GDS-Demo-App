# Azure Pipeline for deploying React/Vite app to Azure Web App
trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

variables:
  # Azure Web App details - update these with your actual values
  azureSubscription: 'YOUR_AZURE_SERVICE_CONNECTION_NAME'
  appName: 'YOUR_WEB_APP_NAME'
  package: '$(System.DefaultWorkingDirectory)/**/*.zip'

stages:
- stage: Build
  displayName: 'Build React Application'
  jobs:
  - job: Build
    displayName: 'Build and Package'
    steps:
    - task: NodeTool@0
      inputs:
        versionSpec: '20.x'
      displayName: 'Install Node.js'

    - task: Npm@1
      inputs:
        command: 'install'
      displayName: 'Install Dependencies'

    - task: Npm@1
      inputs:
        command: 'custom'
        customCommand: 'run build'
      displayName: 'Build React Application'

    - task: ArchiveFiles@2
      inputs:
        rootFolderOrFile: '$(System.DefaultWorkingDirectory)/dist'
        includeRootFolder: false
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/app.zip'
        replaceExistingArchive: true
      displayName: 'Archive Build Output'

    - task: PublishBuildArtifacts@1
      inputs:
        PathtoPublish: '$(Build.ArtifactStagingDirectory)/app.zip'
        ArtifactName: 'drop'
        publishLocation: 'Container'
      displayName: 'Publish Artifacts'

- stage: Deploy
  displayName: 'Deploy to Azure Web App'
  dependsOn: Build
  condition: succeeded()
  jobs:
  - job: Deploy
    displayName: 'Deploy to Azure Web App'
    steps:
    - task: DownloadBuildArtifacts@1
      inputs:
        buildType: 'current'
        downloadType: 'single'
        artifactName: 'drop'
        downloadPath: '$(System.ArtifactsDirectory)'
      displayName: 'Download Artifacts'

    - task: AzureWebApp@1
      inputs:
        azureSubscription: '$(azureSubscription)'
        appType: 'webApp'
        appName: '$(appName)'
        package: '$(System.ArtifactsDirectory)/drop/app.zip'
      displayName: 'Deploy to Azure Web App'
